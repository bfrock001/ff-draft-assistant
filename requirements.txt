# Phase 0 foundation + data loaders (installed and in use now)
numpy>=1.26
pandas>=2.0
pytest>=8.0
polars>=1.0
nflreadpy>=0.1

# Later phases (install before wiring the Streamlit UI)
# streamlit>=1.30
